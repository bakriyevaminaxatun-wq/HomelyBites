# HomelyBites

A simple registration and login page for a home cooked food ordering app.

## Built With
- HTML, CSS, JavaScript
- Node.js, Express.js
- MySQL

## How to Run
1. Run `npm install`
2. Add your MySQL credentials to a `.env` file
3. Run `node server.js`
4. Open `index.html` in your browser